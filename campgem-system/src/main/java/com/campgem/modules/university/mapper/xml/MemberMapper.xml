<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campgem.modules.university.mapper.MemberMapper">
	<update id="updateUserShoppingMethods">
		UPDATE member
		SET shipping_methods = #{vos, typeHandler=com.campgem.common.typehandler.JsonTypeHandler}
		WHERE user_base_id = #{uid}
		  AND del_flag = 0
	</update>
	
	<select id="queryShoppingMethods" resultType="java.lang.String">
		SELECT shipping_methods
		FROM member
		WHERE user_base_id = #{uid}
	</select>
</mapper>